// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

// export default nextConfig;
// Next.js configuration file
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
